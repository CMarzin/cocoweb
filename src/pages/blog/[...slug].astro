---
import { getCollection, render } from 'astro:content';
import PostLayout from '../../layouts/PostLayout.astro';
import { useTranslations } from '../../i18n/utils';

const t = useTranslations('en');

export async function getStaticPaths() {
  let posts = await getCollection('blog', ({ id }) => id.startsWith('en/'));
  let isFallback = false;
  
  if (posts.length === 0) {
    posts = await getCollection('blog', ({ id }) => id.startsWith('fr/'));
    isFallback = true;
  }
  
  return posts.map((post) => ({
    params: { slug: post.id.replace(/^(en|fr)\//, '') },
    props: { post, isFallback },
  }));
}

const { post, isFallback } = Astro.props;
const { Content } = await render(post);
---

<PostLayout frontmatter={post.data} slug={post.id} lang="en" showTranslationNotice={isFallback}>
  <Content />
</PostLayout>
